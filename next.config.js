/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cdn-icons-png.flaticon.com', 'cdn.pixabay.com', 'img.freepik.com', 'media.istockphoto.com']
  }
}

module.exports = nextConfig
